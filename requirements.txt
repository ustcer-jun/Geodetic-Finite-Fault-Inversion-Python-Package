# Core dependencies for your project
# Core numerical stack
numpy>=1.23,<2.3
scipy>=1.9,<1.13
matplotlib>=3.6,<3.9

# Geoscience / mapping
pyproj>=3.4
xarray>=2022.6

# GMT interface (optional but recommended)
pygmt>=0.8

# System utilities
psutil>=5.9
# Jupyter environment
jupyterlab==4.4.10
notebook==7.4.7
ipykernel==7.0.1
matplotlib-inline==0.2.1

# Optional: for reproducibility and system dependencies
# pygmt requires GMT 6.x installed on the system:
# macOS: brew install gmt
# Linux: sudo apt install gmt gmt-dcw gmt-gshhg
# Conda users: conda install -c conda-forge pygmt gmt
